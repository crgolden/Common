namespace Common
{
    /// <summary>Database provider types.</summary>
    public enum DatabaseType
    {
        /// <summary>The SQL server provider type.</summary>
        SqlServer,

        /// <summary>The SQLite provider type.</summary>
        Sqlite
    }
}
